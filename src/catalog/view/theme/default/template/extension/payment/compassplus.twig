<form action="{{ host }}" method="GET">
    <input type="hidden" name="ORDERID" value="{{ compassplus_order_id }}">
    <input type="hidden" name="SESSIONID" value="{{ session_id }}">

    <div class="buttons">
        <div class="pull-right">
            <input type="submit" value="{{ button_confirm }}" id="button-submit"
                   data-loading-text="" class="btn btn-primary"/>
        </div>
    </div>
</form>